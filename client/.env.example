# Google OAuth Client ID for the "Sign in with Google" button.
# Must match GOOGLE_CLIENT_ID in server/.env. Get one from Google Cloud Console
# (Web application; add http://localhost:5173 as an authorized JavaScript origin).
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id
